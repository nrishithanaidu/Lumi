[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Custom markers
markers =
    integration: marks tests that require real AWS credentials and deployed resources

# Default options
addopts =
    --tb=short
    -q
